import type { Metadata } from 'next'
import { Geist, Geist_Mono } from 'next/font/google'
import { Analytics } from '@vercel/analytics/next'
import './globals.css'

const _geist = Geist({ subsets: ["latin"] });
const _geistMono = Geist_Mono({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: 'Blockstreet - Web3 Airdrop Platform | Claim BLOCK Tokens',
  description: 'Access exclusive token airdrops on Blockstreet. Claim your BLOCK tokens and participate in the future of decentralized finance with enterprise-grade infrastructure.',
  generator: 'v0.app',
  metadataBase: new URL('https://blockstreet.xyz'),
  openGraph: {
    title: 'Blockstreet - Web3 Airdrop Platform',
    description: 'Exclusive early access to BLOCK token and curated airdrops',
    url: 'https://blockstreet.xyz',
    type: 'website',
  },
  icons: {
    icon: [
      {
        url: '/icon-light-32x32.png',
        media: '(prefers-color-scheme: light)',
      },
      {
        url: '/icon-dark-32x32.png',
        media: '(prefers-color-scheme: dark)',
      },
      {
        url: '/icon.svg',
        type: 'image/svg+xml',
      },
    ],
    apple: '/apple-icon.png',
  },
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en" className="dark bg-background">
      <body className="font-sans antialiased">
        {children}
        {process.env.NODE_ENV === 'production' && <Analytics />}
      </body>
    </html>
  )
}
